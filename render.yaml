services:
  - type: web
    name: sogileave
    env: java
    buildCommand: ./gradlew build
    startCommand: java -jar build/libs/*.jar
    envVars:
      - key: PORT
        value: 8080
      - key: KTOR_ENV
        value: production
    autoDeploy: true
    healthCheckPath: /health
    region: frankfurt